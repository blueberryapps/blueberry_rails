set :stage,     :staging
set :deploy_to, '/var/www/<%= app_name %>_webapps_cz'
set :rails_env, 'staging'

server 'REPLACE_ME',
        user: '<%= app_name %>_webapps_cz',
        roles: %w(web app db)


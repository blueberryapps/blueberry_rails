language: ruby
cache: bundler
sudo: false

rvm:
  - <%= BlueberryRails::RUBY_VERSION %>

install:
  - bin/setup

branches:
  only:
    - master

addons:
  postgresql: '9.3'

env:
  global:
    - RAILS_ENV=test
